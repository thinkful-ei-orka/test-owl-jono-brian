import React from 'react'
import participantsList from './participantsList'

export default function onStageList(props){
    const onStage  = props.filter(prop => prop.onStage === true);
    
    //console.log (onStage);
};

